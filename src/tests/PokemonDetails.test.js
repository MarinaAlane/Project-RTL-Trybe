import React from 'react';
import userEvent from '@testing-library/user-event';
import renderWithRouter from './renderWithRouter';
import PokemonDetails from '../components/PokemonDetails';
import pokemons from '../data';

describe('Requirement 07', () => {
  it('should be forgotten :\'( ', () => {});
  // const [pikachu, charmander] = pokemons;
  // const favoriteByID = { 25: true, 4: false };
  // it('should contain detailed info about the selected Pokemon', () => {
  //   const { getByRole } = renderWithRouter(<PokemonDetails
  //     isPokemonFavoriteById={ favoriteByID[pikachu.id] }
  //     match={ 'match' }
  //     pokemons={ pikachu }
  //   />);
  //   // const 
  //   // expect(name).toBeInTheDocument();
  //   // expect(summaryHeader).toBeInTheDocument();
  //   // expect(summary).toBeInTheDocument();
  // });
});
